#include <fstream>
#include <iostream>
using namespace std;

int main(){
  string x,y,z,a;
  ifstream myIn("output.txt");
  if(myIn==0) exit(0);  
  cout << "The file starts with ";
  getline(cin, x);    
  cout  << x << " ";
 return 0;
}
