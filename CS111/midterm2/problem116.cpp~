#include <iostream>
using namespace std;

int length(int x)
{
  if(x<10)
    return 1;
  return 1+length(x/10)
    }
int main(){
  int n;
  cout << "Entera positive integer: ";
  cin >> n;

  cout << "Digits in n: " << length(n);
  
  return 0;
}
