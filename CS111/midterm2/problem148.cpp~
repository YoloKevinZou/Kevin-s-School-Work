#include <iostream>
using namespace std;

int main(){
  int n;
  cout << "Enter a positive integer: ";
  cin >> n;
  string names[n];
  for(int i = 0; i < n; i++)
    {
      cin >> names[i];
    }
  
  for(int i = n-1; i>=0; i--)
    {
      cout << names[i] << " ";
    }
  return 0;
}
